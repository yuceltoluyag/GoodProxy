
</think>

# GoodProxy

A lightweight proxy checker that validates HTTP and SOCKS proxies against YouTube.

## Usage
1. Add your proxy list to `raw.txt` (formats: `IP:PORT` or `IP:PORT:USER:PASS`).
2. Run the script: `python control.py`
3. Verified proxies will be saved to `GoodProxy.txt`.
